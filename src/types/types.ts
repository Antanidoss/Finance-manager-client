export type DailyReportType = {
    id: number,
    timeOfCreate: string,
}

export type ReportType = {
    id: number,
    amountSpent: number,
    descriptionsOfExpenses: string
    timeOfCreate: string
}